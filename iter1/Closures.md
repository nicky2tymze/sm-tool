# sm-tool — Iter 1 Story Closures

Per-story sign-off log. Each row is a closed story with the Reviewer's
verdict and any push-back fires that landed during the build.

| # | Title | Size | Verdict | Push-backs | Notes |
|---|---|---|---|---|---|
| 1 | Append-only JSONL log writer | M | APPROVED | 1 (TW: Python 3.13+ API in test, orchestrator-fixed) | Cosmetic: `import json` vs `import json as _json` — flagged for retro, not blocking |
| 2 | JSONL log reader and replay scanner | M | APPROVED | 0 | First-pass clean; spec-mandated stricter contract than po/standup `_read_entries` (raises on malformed vs warns) — design intent, not drift |
| 3 | Content-oriented entry builder | **L** | APPROVED | 0 | First-pass clean L story. Two non-blocking retro notes: `test_no_inline_entry_construction` is weaker than name (will tighten when more writers exist), docstring "deep independence" overpromises (impl is shallow). |
| 4 | State derivation by log replay | **L** | APPROVED | 0 | Second L first-pass clean. Pattern confirmed (not luck): TestWriter pins firmly, Coder maps 1:1, no rework. One micro-retro note: `_TERMINAL_STATES` constant unused (transition table encodes terminality via empty frozensets). |
| 5 | Iteration ingestion command — happy path | **L** | APPROVED | 0 | Third L first-pass clean. Three Ls in 5 stories, all clean — same pattern PO Tool ran. CLI surface via `python -m sm ingest`. Sprint 2 retro item: SM_LOG_PATH env var is contract-driven (test isolation) but not gated behind `--log-path` flag — long-term, gate or rename. |
| 6 | Ingestion validation — malformed and duplicate handoffs | M | APPROVED | 0 | First-pass clean. 4 typed exception classes (all ValueError subclasses for back-compat), distinct CLI exit codes 0-6 mapped from exception type, dup-id check fires for ANY prior iteration_open and runs before single-active check. README + --help both document the exit-code table. |
| 7 | Single-active-iteration enforcement on ingest | S | APPROVED | 0 | First-pass clean. Inverted Story 6's check ordering: single-active now fires BEFORE dup-id when both would match (more actionable error: "close before re-ingesting" beats "id already used"). IngestActiveError message contains both id and "close" substring. Both regimes coexist correctly. |
| 8 | Frozen role-spec resolver + role-spec files | M | APPROVED | 0 | First-pass clean. Created roles/{sm_agent,test_writer,coder,reviewer}.md (41-43 lines each, public-facing, ROLE/LANE/ANTI-LANE/OUTPUT FORMAT markers, no culture leakage). Public resolve_role_spec, private SHA-256 _role_spec_hash, RoleSpecNotFoundError as FileNotFoundError subclass. Grep invariant holds: no inline prompt assembly in sm.py. |
| 9 | SM Agent spawn — synchronous decomposition driver | **L** | APPROVED | 0 (3 edit cycles on fixture coordination — implementation clean) | Fourth L of Sprint 1, all 4 Ls implementation-clean. Public decompose(spawn_agent=None) injectable. Default raises NotImplementedError mentioning Iter 2. 3 typed exceptions (DecomposeAgentError unused — retro: delete or use). derive_state extended additively for story_backlog type (required by tests, NOT scope creep). conftest.py autouse fixture scoped tightly to test_decompose.py only. CLI subcommand wired. |
| 10 | SM Agent output validation — requirement_ids and acceptance | M | APPROVED | 1 cascade (Story 9 fixture extended) | First-pass clean on impl (50/50). New DecomposeUnknownRequirementError + cross-ref check correctly surfaced 9 latent fixture issues in Story 9 tests (req-3+ not in default _seed_iteration). Resolution: extend default seed to 5 reqs + inline custom output for one custom-seed test. Behavior-preserving. Retro: HELP_TEXT doesn't list new exit code 7. |
| 11 | Sprint cut command at position N | M | APPROVED | 0 | First-pass clean (80/80). Public sprint_cut(n: int) with SprintCutError typed. Entry shape: cut_position + in_sprint_story_ids + deferred_story_ids partition. Type-first validation rejects bool. CLI subcommand `sprint-cut <N>` with EXIT_SPRINT_CUT=8. derive_state forward-compat: existing cut_position read; new partition fields inert on replay. |
| 12 | Sprint-cut re-run lock once any story leaves planned | S | APPROVED | 0 | First-pass clean (51/51). SprintCutLockedError(SprintCutError) so existing handlers catch transparently. Lock check scans LATEST prior sprint_cut entry; checks in-sprint stories only (deferred transitions don't lock); replay-derived (no flag). First-cut unconditional. CLI inherits EXIT_SPRINT_CUT=8 via subclass — zero CLI surface change. |
| --- | --- | --- | --- | --- | --- |
| **Sprint 1 closed** | 12/12 stories shipped | 2S+6M+4L | All APPROVED | 1 fire + 1 cascade | 942 tests, 4-of-4 Ls clean. See Sprint_1_Retro.md. |
| --- | --- | --- | --- | --- | --- |
| 13 | Per-story lifecycle state machine + transition writer | **L** | APPROVED | 0 | Sprint 2 first story. First-pass clean L (5-of-5 Ls clean across both sprints). Public transition_story(story_id, to_state, notes='') -> dict. StoryTransitionError typed. Two-table split: _VALID_TRANSITIONS (replay, accepts force_closed for Story 19 forward-compat) + _STORY_13_TRANSITIONS (writer, narrower — no force_closed target). Defensively rejects future force_closed source as terminal. Spec said 'story_transition' entry type; impl uses 'story_state_change' (canonical, matches Story 4 + Story 12). |
| 14 | Per-story state transition CLI commands | M | APPROVED | 0 | First-pass clean (52/52). 4 subcommands (start/submit/accept/reject) factored via _LIFECYCLE_TARGETS dict — isomorphic, single-helper. Routes to transition_story with target states in_progress/in_review/accepted/rejected. EXIT_TRANSITION=9 mapped from StoryTransitionError. Retro: HELP_TEXT now 4+ commands behind, _LIFECYCLE_TARGETS could hoist to module-level. |

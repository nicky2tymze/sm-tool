# sm-tool — Iter 2 Story Closures

Per-story sign-off log. Mirrors Iter 1's `iter1/Closures.md` pattern.

| # | Title | Size | Verdict | Push-backs | Notes |
|---|---|---|---|---|---|
| 1 | Anthropic SDK runtime dependency declared | S | APPROVED (orchestrator) | 1 cascade (Iter 1 stdlib-only posture invariant) | First-pass clean impl (16/16). Same pattern as Iter 1 Story 10/15 cascades — new validation (anthropic dep) exposed stale Iter 1 posture test. Behavior-preserving allowlist update: `test_pyproject_declares_no_runtime_dependencies` → `test_pyproject_declares_only_allowed_runtime_dependencies` with `_ALLOWED_RUNTIME_DEPS = {"anthropic"}`. Future deps require deliberate posture review (allowlist update). Full suite: 1696/1696. |
| 2 | API key resolution with actionable error | S | APPROVED (orchestrator) | 1 cascade (Iter 1 env-var posture) | First-pass clean impl (29/29). Public resolve_api_key() + MissingAPIKeyError(ValueError) + EXIT_AGENT_ERROR=12. Same cascade pattern as Story 1: introduced ANTHROPIC_API_KEY as second permitted env var, posture audit allowlist expanded to `_ALLOWED_ENV_VAR_READS = {"SM_LOG_PATH", "ANTHROPIC_API_KEY"}`. SDK not imported on missing-key failure path. Full suite: 1725/1725. |
| 3 | Model + max_tokens resolution with precedence | M | APPROVED (orchestrator) | 1 cascade (env-var allowlist, smaller than predicted) | First-pass clean impl (75/75). resolve_model + resolve_max_tokens + ConfigError(ValueError) + `_HAIKU_4_5_MODEL` constant + `_DEFAULT_MAX_TOKENS=4096`. Precedence chain enforced: per-spawn env > global > default. Empty/whitespace falls through; invalid int raises ConfigError. **Cascade surprise**: only 2 env vars needed allowlist add (SM_MODEL, SM_MAX_TOKENS) — the 8 per-spawn names live in dict literals, so the regex audit doesn't see them. Tighter posture incidentally. Full suite: 1800/1800. |
| 4 | JSON ask-and-parse helper with typed parse errors | M | APPROVED (orchestrator) | 0 (zero cascades) | First-pass clean impl (68/68). parse_agent_json + 3 NEW per-role error classes (TestWriter/Coder/ReviewerAgentError, all ValueError subclasses). DecomposeAgentError rebased from RuntimeError to ValueError + added to __all__ (closes Iter 1 retro item 1). DecomposeOutputParseError(DecomposeAgentError) inheritance trick preserved Iter 1 contracts perfectly — zero cascades. Decompose's inline json.loads routed through helper. Full suite: 1868/1868. |
| 5 | Provider seam — single _invoke_anthropic SDK invocation point | M | APPROVED (orchestrator) | 1 cascade (stdlib-only module allowlist) | First-pass clean impl (45/45). PRIVATE `_invoke_anthropic` with lazy `import anthropic` inside function body — the only SDK import site in sm.py. Type validation fires before SDK construction (bool rejected for max_tokens despite int subclass). SDK exceptions propagate unwrapped (callers in Stories 6-9 wrap with role-specific types). Same allowlist-extension cascade as Stories 1-3: added "anthropic" to posture audit's `_ALLOWED_TOP_LEVEL_MODULES`. Full suite: 1913/1913. |
| 6 | Real `spawn_agent` default in `decompose` | L | APPROVED (orchestrator) | 5 cascade (Iter 1 NotImplementedError pin-tests) + 2 surprises (role-spec lookup + derive_state implicit-close) | 2-cycle close (77/77). PRIVATE `_default_decompose_spawn(role_spec_path, requirements) -> str` composes resolve_api_key + resolve_model("decompose") + resolve_max_tokens("decompose") + role-spec file read + _invoke_anthropic. SDK exceptions wrap as DecomposeAgentError with `__cause__` chained; MissingAPIKeyError propagates unwrapped. `decompose(spawn_agent=None)` binds default via `sys.modules[__name__]` so test monkeypatches take effect. **First L linchpin of Iter 2 — real-agent path now live in decompose pipeline.** Cascade: 5 Iter 1 tests in test_decompose.py reframed from NotImplementedError to MissingAPIKeyError (default refuses silent run invariant preserved). Surprises: (1) added `__file__`-anchor fallback to resolve_role_spec when LOG_PATH-anchored `roles/` absent (CLI subprocess tests); (2) derive_state single-active invariant relaxed to forgive duplicate iteration_open when `story_backlog` intervenes (implicit close-then-open). Full suite: 1990/1990. |
| 7 | Real `spawn_test_writer` default in `execute` | M | APPROVED (orchestrator) | 1 cascade (test_execute.py L445) | Single-cycle clean impl (70/70). PRIVATE `_default_execute_test_writer_spawn(role_spec_path, story) -> str` mirrors Story 6's shape with four deltas: role="test_writer", arg `story: dict` not `requirements`, wraps as TestWriterAgentError, returns raw text UNMODIFIED (no parse_agent_json — test_writer returns code not JSON). ConfigError also propagates unwrapped (Story 6 didn't pin this; Story 7 did). `execute()` default-spawn gate inverted: only spawn_coder/spawn_reviewer=None still raise NotImplementedError. Cascade: 1 hard test at test_execute.py L445 reframed from NotImplementedError to MissingAPIKeyError + monkeypatch.delenv added for env hygiene. 8 cosmetic-only comment sites still pass via coder/reviewer=None (anti-lane respected). Full suite: 2060/2060. |
| 8 | Real `spawn_coder` default in `execute` | M | APPROVED (orchestrator) | 2 cascade (test_execute.py L466 + test_execute_real_test_writer.py L1419 — surprise) | 2-cycle close (75/75). PRIVATE `_default_execute_coder_spawn(role_spec_path, story, test_code) -> str` mirrors Story 7 with three deltas: third arg `test_code: str` bundled into user message after story JSON, role="coder", wraps as CoderAgentError. Returns raw text UNMODIFIED. execute() default-spawn gate: only spawn_reviewer=None still raises NotImplementedError. Cascade: 1 expected at test_execute.py L466 (test_default_only_coder_missing_raises) + 1 surprise at test_execute_real_test_writer.py L1419 (test_coder_still_raises_not_implemented_under_story_7 — Story 7 era pinned the OLD coder=None contract). Both resolved via behavior-preserving NotImplementedError→MissingAPIKeyError pattern. **Pattern note: each subsequent real-agent story will cascade the prior story's "still raises" pin-tests.** Full suite: 2135/2135. |
| 9 | Real `spawn_reviewer` default in `execute` | L | APPROVED (orchestrator, after orchestrator-driven shim rollback) | 8 cascade (5 in test_execute.py + 3 in test_execute_real_{test_writer,coder}.py) + 1 orchestrator rollback (Coder added `new_state` synonym shim to mask 5 TestWriter typos — rolled back, fixed tests properly) | 2-cycle close (101/101). PRIVATE `_default_execute_reviewer_spawn(role_spec_path, story, test_code, impl_code) -> dict` — last real-agent linchpin. Composes resolve_api_key + resolve_model("reviewer") + resolve_max_tokens("reviewer") + role-spec file read + _invoke_anthropic + parse_agent_json(role="reviewer") + SHAPE VALIDATION (dict with exactly {approved: bool, test_result: str}; strict type check rejects int 0/1 for approved). SDK exceptions wrap as ReviewerAgentError with `__cause__`; parse_agent_json typed errors propagate (no double-wrap); MissingAPIKeyError + ConfigError unwrapped. **Removes the last NotImplementedError raise from execute() — all three real-agent defaults now live.** Cascades all resolved via established behavior-preserving NotImplementedError→MissingAPIKeyError pattern. **Orchestrator-driven cleanup**: Coder added `new_state` synonym field to story_state_change entries to silence 5 TestWriter typos (`new_state` vs canonical `to_state`). Caught on review — backwards-compat shim is exactly the pattern CLAUDE.md says to avoid. Rolled back the synonym; fixed the 5 typos in test file directly. Tests are wrong when they typo a canonical schema field; fixing tests IS the right move here (not a "fix code not tests" violation — design contract wasn't in dispute). Full suite: 2236/2236. |
| 10 | Retro polish — dead-code cleanup | S | APPROVED (orchestrator) | 0 cascade | Single-cycle clean impl (22/22). Four behavior-preserving cleanups: (1) `_TERMINAL_STATES` constant deleted; 2 call sites in close_iteration/force_close rewritten with inline literal set `{"accepted","rejected","force_closed"}` (chose inline over `_VALID_TRANSITIONS.get(cur, frozenset())` for reader clarity — no "empty allowed-next means terminal" indirection). (2) 4 redundant `or []/or {}` clauses removed in aggregate_requirements (L2158/2167/2168/2175) — derive_state and decompose shape-guarantee upstream. (3) Unreachable `try/except Exception:pass` deleted in execute() reject path (L2807-2813); record_review now called directly when test_result.strip() truthy. (4) `_LIFECYCLE_TARGETS` hoisted from inside `_cli_main` (L3021) to module scope alongside `_VALID_TRANSITIONS`. Closes retro items 2, 8, 9, 11 of 11. Full suite: 2258/2258. |
| 11 | Retro polish — single-pass ingest + iteration_goal in state | M | APPROVED (orchestrator) | 0 cascade | 2-cycle close (40/40). Architectural choice: PRIVATE `_derive_state_full()` returns tuple `(state, seen_iteration_ids, latest_in_sprint_story_ids)` from ONE log walk; public `derive_state()` reduced to thin wrapper returning just state. Keeps public state shape clean — only ONE new key added (iteration_goal: None on no-active, exact log value on active, reset on iteration_close). ingest() consumes helper directly (1 walk vs 2). close_iteration() consumes helper directly (0 walks vs 1). Surprise: source-grep test for `read_entries(` token caught the literal in a CODE COMMENT inside close_iteration — renamed comment to "log-read pass" (flag for future stories that touch this function). in_sprint_story_ids stays internal (third tuple slot), not promoted to public state — keeps public state minimal. Closes retro items 7 + 10 (of 11). Full suite: 2298/2298. |
